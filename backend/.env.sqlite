# Database Configuration (SQLite para desarrollo rápido)
DATABASE_TYPE=sqlite
DATABASE_NAME=./admin_panel.db

# Database Configuration (PostgreSQL - comentado por ahora)
# DATABASE_TYPE=postgres
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USERNAME=admin
# DATABASE_PASSWORD=admin123
# DATABASE_NAME=admin_panel_db

# JWT Configuration
JWT_SECRET=tu-jwt-secret-super-secreto-y-largo-para-desarrollo-2024
JWT_EXPIRES_IN=24h

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:4200

# Swagger Configuration
SWAGGER_TITLE=Admin Panel API
SWAGGER_DESCRIPTION=API REST para sistema de gestión comercial
SWAGGER_VERSION=1.0.0
